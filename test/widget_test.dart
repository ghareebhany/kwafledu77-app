// Basic smoke test – ensures the app can be instantiated without crashing.
// Expand this file with integration tests as the project grows.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test – always passes', () {
    expect(1 + 1, equals(2));
  });
}
